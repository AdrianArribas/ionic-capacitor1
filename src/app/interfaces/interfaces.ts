export interface MenuItem {
    icon: string;
    name: string;
    redirectTo: string;
}

export interface Heroe {
    superhero: string;
    publisher: string;
    alter_ego: string;
    first_appaerance: string;
    characters: string;
}